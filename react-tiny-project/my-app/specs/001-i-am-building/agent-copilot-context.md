```markdown
# Agent context — copilot

This file documents the technologies used in the implementation plan for the copilot agent.

- Framework: Next.js (App Router)
- Data: Embedded mock JSON (`data/episodes.json`) — static generation at build time
- Styling: Tailwind / plain CSS (project contains Tailwind import)
- No external APIs or runtime data sources

Use this context to inform code-generation or further planning tasks.

```