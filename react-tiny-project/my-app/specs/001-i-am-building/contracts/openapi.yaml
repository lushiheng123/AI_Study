openapi: 3.0.0
info:
  title: Podcast Site (placeholder)
  version: 0.1.0
paths:
  /episodes:
    get:
      summary: List episodes (placeholder)
      responses:
        '200':
          description: OK
  /episodes/{slug}:
    get:
      summary: Get episode by slug (placeholder)
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: OK
